import React from 'react';

/**
 * Deprecated floating language popup has been removed.
 * Language switching is now cleanly integrated directly into the Navbar and Auth pages.
 */
export const LanguageSelectionModal: React.FC = () => {
  return null;
};
